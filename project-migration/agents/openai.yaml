interface:
  display_name: "前端项目迁移"
  short_description: "以可验证方式迁移前端页面、功能与技术栈"
  default_prompt: "使用 $project-migration 处理这个前端迁移任务。先判断当前是评估、规划、执行还是验收，并检查工作树与真实迁移前状态；运行前端盘点脚本，建立最小迁移包；按功能、视觉、交互、响应式与平台契约设计目标原生实现；采用试迁移和小步验证，以可复现证据结束。不要把源组件树一对一翻译，也不要未经批准 redesign。"
