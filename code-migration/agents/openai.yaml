interface:
  display_name: "代码迁移"
  short_description: "按功能语义理解源项目，并依据目标架构进行多项目融合与增量迁移"
  default_prompt: "使用 $code-migration 时先检查目标仓库根目录 `.code-migration/` 恢复已有迁移进度、用户决策和待澄清问题；若不存在再建立迁移上下文，遇到会影响方向的 Unknown 可循环向用户澄清，然后按结构切片和可验证功能切片逐步重建、融合并完成切流清理。"
