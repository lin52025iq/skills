# 00 模块总览

> `modules/<module>/` 的入口文件。用于快速查看模块目标、Feature 完成度、当前焦点、阻塞和相关文档。详细 Feature / Atomic Item 状态以 `01-模块功能点.md` 为权威源。

目标工作区生成本文件时，在标题下创建双向导航，目标路径为：

```text
返回全局状态 → ../../00-迁移状态总览.md
返回模块索引 → ../../03-迁移模块索引与进度.md
```

生成到目标工作区后，再将这些路径转换为真实 Markdown 链接。

## 模块信息

- **Module ID**：
- **模块名称**：
- **风险级别**：`低风险 / 中风险 / 高风险`
- **模块状态**：`未开始 / 进行中 / 验证中 / 已完成 / 已阻塞`
- **用户目标**：
- **用户新功能 / UX / 布局方案**：
- **目标模块 / 区域**：
- **当前 Workstream**：
- **下一安全动作**：

## Feature 完成投影

> Checkbox 只由 Feature 生命周期派生：`已迁移 / 已清理 → [x]`，其他状态全部 `[ ]`。

```text
[ ] F-001 功能名称 — 实施中 · 2/4 Atomic Item
    详情：01-模块功能点.md#f-001-功能名称
[x] F-002 已完成功能 — 已迁移
    详情：01-模块功能点.md#f-002-已完成功能
```

目标工作区生成时，可把 Feature 名称转换为指向对应锚点的真实 Markdown 链接。

## 模块摘要

- **Feature 总数**：
- **已迁移**：
- **进行中**：
- **验证中**：
- **验证失败 / 阻塞**：
- **模块集成验证**：`不要求 / 未验证 / 验证中 / 通过 / 失败`
- **最大未解决问题**：

## 当前焦点

- **当前 Feature**：
- **当前 Atomic Item(s)**：
- **当前验证场景**：
- **阻塞 / 失败**：

## 模块文档

- [01 模块功能点](01-模块功能点.md)
- [02 功能语义](02-功能语义.md) — `按需`
- [03 源行为对齐](03-源行为对齐.md) — `按需`
- [04 页面结构与交互](04-页面结构与交互.md) — `按需`

目标工作区只保留实际存在的可选文档链接；未创建的可选文档不要留下死链接。

## 模块完成 Gate

- [ ] 所有范围内必需 Feature 达到目标生命周期；
- [ ] 所有必需 Atomic Item 均完成；
- [ ] 要求的源行为 / 页面结构 / 交互验证通过；
- [ ] 无未处理验证失败或阻塞；
- [ ] 模块共享边界集成验证通过；
- [ ] 模块目标与用户新方案整体成立。

全部满足后，模块状态才可派生为 `已完成`，并同步全局 `03` 的 Module Checkbox 为 `[x]`。
