# 迁移模块功能点清单

> 这是迁移范围与完成度的功能级台账，独立于 Slice Queue。Slice Queue 负责“现在做什么”，本文件负责“完整有哪些模块/功能/子功能项，以及每一项做到什么程度”。开发过程中必须持续更新，不能等一个大 Feature 结束后才补记录。

## 层级模型

```text
Module
  └─ Feature
       └─ Feature Item / 子功能项
```

- **Module**：业务或页面区域，例如“订单中心”“Dashboard”。
- **Feature**：可独立描述用户目标的能力，例如“订单详情”“退款审批”。
- **Feature Item**：Feature 内需要实现或验证的具体细项，例如“基础信息展示”“403 权限处理”“Loading 状态”“退款原因校验”。
- **Slice**：执行调度单位，可以覆盖一个完整 Feature，也可以只覆盖一个 Feature 中明确的一组 Feature Items；Slice 本身不是功能范围的唯一事实源。

## 使用规则

1. 业务模块、跨页面迁移、多项目融合和长周期迁移默认创建本文件；单组件或很小的单次任务可省略。
2. 在大规模开发前先完成第一版 Module → Feature → Feature Item 盘点；允许随着理解加深增删或拆分，但需要同步依据。
3. 每个 Feature 必须有稳定 Feature ID；重要 Feature Item 也应有稳定 Item ID，便于跨会话引用。
4. Slice Queue 不能替代本清单。即使当前没有安排开发，也要保留未开始、Deferred、Remove、Reuse Target 等功能项。
5. 开始开发某个 Feature Item 时立即更新其实施状态；不要等 Slice 完成后批量补状态。
6. 一个 Feature 只有在其所有 IN_SCOPE 且 Required 的 Feature Items 都达到对应完成标准后，才能进入 `MIGRATED`。
7. `REMOVE / DEFER / REUSE_TARGET` 等不产生新代码的能力也保留在清单中，并记录决策/依据。
8. 完成一个功能开发或验证动作后，同时更新：Feature Item → Feature 汇总 → Module 汇总 → 当前执行计划；长周期任务再同步 `00-迁移状态总览.md`。

## Feature 生命周期

```text
DISCOVERED → DESIGNED → IMPLEMENTING → VERIFYING → MIGRATED → ELIMINATED
```

- `DISCOVERED`：已确认属于迁移范围；
- `DESIGNED`：Desired Target Behavior、Target Mapping 和验证方式已经明确；
- `IMPLEMENTING`：至少一个 Required Feature Item 正在开发，且仍有开发项未完成；
- `VERIFYING`：Required 开发项基本完成，但仍有验证项未通过；
- `MIGRATED`：所有 Required Feature Items 均完成，并满足 Feature Done Gate；
- `ELIMINATED`：目标能力完成且旧实现/入口/临时结构已安全退出。

`BLOCKED` 是附加状态，不替代生命周期。

## Feature Item 状态

Feature Item 使用更细的实施状态：

```text
NOT_STARTED → IMPLEMENTING → VERIFYING → DONE
```

并允许：

- `BLOCKED`：有明确阻塞；
- `N/A`：经确认目标不需要该项；
- `DEFERRED`：明确不在当前迁移阶段处理。

`DONE` 必须代表该 Item 的实现和要求的验证均完成；“代码写完但未验证”只能是 `VERIFYING`。

## 模块总览

| Module ID | 模块名称 | 来源 | Target Module / Region | Feature 数 | MIGRATED | ELIMINATED | 当前状态 | 当前 Feature | 备注 |
|---|---|---|---|---:|---:|---:|---|---|---|
| M-001 |  |  |  | 0 | 0 | 0 | `DISCOVERED / DESIGNED / IMPLEMENTING / VERIFYING / MIGRATED / ELIMINATED` |  |  |

模块状态只是摘要，不得覆盖 Feature/Item 的真实状态。

---

## M-001 模块名称

### 模块目标

- Source Evidence Summary：
- Desired Target Behavior Summary：
- Target Module / Region：
- 允许差异 / APPROVED_CHANGE：
- Do Not Touch：

### Feature 总览

| Feature ID | Feature | 来源 | 迁移决策 | Target 落点 | 生命周期状态 | Blocked | 完成 Item / Required Item | 验证摘要 | 关联 Decision / 详细文档 |
|---|---|---|---|---|---|---|---|---|---|
| F-001 |  |  | `KEEP / ADAPT / MERGE / SPLIT / REMOVE / REPLACE / REUSE_TARGET / NEW / DEFER / UNKNOWN` |  | `DISCOVERED` | no | 0 / 0 |  |  |

---

### F-001 Feature 名称

#### Feature 目标

- 用户目标：
- Desired Target Behavior：
- Migration Decision：
- Target Mapping：
- Definition of Done：

#### Feature Item 清单

| Item ID | 子功能项 | 类型 | Required | 来源/依据 | 目标行为或实现要求 | 实施状态 | 验证方式 | 验证结果 | Blocked / Notes |
|---|---|---|---|---|---|---|---|---|---|
| F-001-I01 |  | `UI / Interaction / Data / Permission / State / Validation / Navigation / Integration / A11y / Performance / Other` | yes |  |  | `NOT_STARTED` |  | `NOT_RUN / PASS / FAIL / N/A` |  |

Feature Item 应覆盖实际会影响功能完整性的内容。前端常见拆法包括：

- 主要展示区域；
- 核心用户操作；
- Data Query / Mutation；
- Loading / Empty / Error / Success；
- 权限与 Disabled；
- 表单校验与错误恢复；
- Route / Deep Link / Back-forward；
- Modal / Drawer / Toast；
- 响应式与关键视觉状态；
- Accessibility；
- 埋点 / Analytics；
- 必需的集成或副作用。

不要为了“清单细”把每个 div、函数、变量都拆成 Item。Feature Item 应对应可以判断“完成/未完成”的产品或工程行为。

#### 当前 Slice 映射

| Slice | 覆盖 Feature Items | 当前状态 | 说明 |
|---|---|---|---|
| S-001 | F-001-I01, F-001-I02 | `READY / NOW / BLOCKED / DONE` |  |

一个 Slice 可以覆盖多个强关联 Item，但完成 Slice 后必须逐项更新 Item 状态，不能只把 Slice 标 DONE。

#### Feature 完成 Gate

- [ ] 所有 Required Feature Items 已识别
- [ ] 所有 Required 开发项均为 `DONE / N/A / DEFERRED(已批准)`
- [ ] 所有要求验证的 Item 均有 `PASS / N/A`
- [ ] Business Rule / Invariant 已验证
- [ ] Permission / Data Contract / Error Path 已覆盖
- [ ] APPROVED_CHANGE 已记录且验证的是目标行为
- [ ] 不存在被 Slice DONE 掩盖的未完成 Item
- [ ] Feature 生命周期状态已同步

## 当前迁移焦点

- NOW Slice：
- Current Feature：
- Current Feature Items：
- READY：
- BLOCKED：
- Next Candidate：

## 模块完成检查

- [ ] 所有 IN_SCOPE Feature 已列入清单
- [ ] 每个 Feature 的 Required Feature Items 已达到足够完整度
- [ ] 所有 Feature 都有迁移决策或明确 `UNKNOWN`
- [ ] `IMPLEMENTING / VERIFYING` Feature 与实际 Item 状态一致
- [ ] `MIGRATED` Feature 没有 Required Item 遗漏
- [ ] `REMOVE / REUSE_TARGET / DEFER` 有明确依据
- [ ] 模块 `MIGRATED` 状态与所有 Feature 状态一致
- [ ] 遗留入口、旧实现、Flag、Adapter 已列出
- [ ] 模块 `ELIMINATED` 前已完成遗留清理 Gate

## 开发时同步更新纪律

开发不是“先写代码，最后补清单”。每次实际工作按以下循环：

```text
选择 Slice / Feature Items
→ 开始开发：相关 Item = IMPLEMENTING
→ 代码主体完成：相关 Item = VERIFYING
→ 执行验证：逐 Item 写 PASS / FAIL
→ 完成：Item = DONE
→ 重新计算 Feature 生命周期
→ 更新 Module 汇总
→ 更新 Slice Queue
→ 长周期任务同步 00-迁移状态总览
→ 选择下一批 Feature Items
```

如果开发中发现新的必要子功能：

1. 立即加入对应 Feature 的 Item 清单；
2. 判断 Required / Optional / Deferred；
3. 判断是否影响当前 Slice 和 Definition of Done；
4. 不得因为最初清单漏写而静默忽略。

如果实际代码状态与本清单冲突，以已确认 Decision、真实代码/Git/Runtime/Test 为依据，先 Reconcile 清单再继续开发。
