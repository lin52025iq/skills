# 迁移模块功能点清单

> 这是迁移范围与完成度的功能级台账，独立于 Slice Queue。Slice Queue 负责“现在做什么”，本文件负责“完整有哪些模块、功能、子功能项，以及每一项做到什么程度”。开发过程中必须持续更新。

## 层级模型

```text
Module
  └─ Feature
       └─ Feature Item / 子功能项
```

- Module：业务或页面区域；
- Feature：可独立描述用户目标的能力；
- Feature Item：Feature 内需要实现或验证的具体细项；
- Slice：执行调度单位；
- Workstream：由一个明确 Owner 负责的分析、实施或验证工作流；多个独立 Workstream 可以并行。

## 使用规则

1. 大规模开发前先完成 Module → Feature → Feature Item 盘点；
2. 每个 Feature / 重要 Item 使用稳定 ID；
3. Slice Queue 不能替代本清单；
4. 开始开发 Item 时立即更新实施状态；
5. 只有所有“范围内 + 必需”的 Items 达标，Feature 才能“已迁移”；
6. 不迁移、延后、复用目标能力也保留并记录依据；
7. 并行开发时，每个实施中的 Feature / Item 必须记录 Workstream 与 Owner；
8. 同一个 Item 同一时刻只能有一个生产代码 Owner。

## 状态维度

### 范围状态

```text
范围内 / 延后 / 范围外
```

### 实施状态

```text
未开始 → 实施中 → 已实施
```

### 验证状态

```text
未验证 → 验证中 → 通过 / 失败 / 不适用
```

### 阻塞状态

```text
未阻塞 / 已阻塞
```

### 是否必需

```text
是 / 否
```

## Feature 生命周期

```text
已发现 → 已设计 → 实施中 → 验证中 → 已迁移 → 已清理
```

- 已发现：功能已识别，但目标行为或 Items 尚不完整；
- 已设计：必需 Items、目标行为、Target Mapping 和验证方式已明确；
- 实施中：仍有必需 Item 未实施；
- 验证中：必需 Items 已实施，但仍有验证未通过/不适用；
- 已迁移：所有“范围内 + 必需=是”Items 已实施且验证通过/不适用；
- 已清理：目标能力完成且旧实现、入口和临时结构已安全退出。

## 模块总览

| Module ID | 模块名称 | Target Module / Region | Feature 数 | 实施中 | 验证中 | 已迁移 | 已清理 | 已阻塞 | 活跃 Workstreams | 备注 |
|---|---|---|---:|---:|---:|---:|---:|---:|---|---|
| M-001 |  |  | 0 | 0 | 0 | 0 | 0 | 0 |  |  |

---

## M-001 模块名称

### 模块目标

- Source Evidence Summary：
- Desired Target Behavior Summary：
- Target Module / Region：
- 允许差异 / APPROVED_CHANGE：
- Do Not Touch：

### Feature 总览

| Feature ID | Feature | 范围状态 | 迁移决策 | Target 落点 | 生命周期状态 | 阻塞状态 | Workstream | 实施 Owner | 验证 Owner | 必需 Items | 已实施 | 验证通过/不适用 | 关联 Decision / 文档 |
|---|---|---|---|---|---|---|---|---|---|---:|---:|---:|---|
| F-001 |  | `范围内` | `KEEP / ADAPT / MERGE / SPLIT / REMOVE / REPLACE / REUSE_TARGET / NEW / DEFER / UNKNOWN` |  | `已发现` | `未阻塞` |  |  |  | 0 | 0 | 0 |  |

---

### F-001 Feature 名称

#### Feature 目标

- 用户目标：
- Desired Target Behavior：
- Migration Decision：
- 范围状态：`范围内 / 延后 / 范围外`
- Target Mapping：
- Definition of Done：
- 当前 Workstream：
- 实施 Owner：
- 验证 Owner：
- 允许修改范围：
- 禁止修改范围：
- 依赖 Workstreams：

#### Feature Item 清单

| Item ID | 子功能项 | 类型 | 范围状态 | 是否必需 | 来源/依据 | 目标行为或实现要求 | Workstream | 实施 Owner | 验证 Owner | 实施状态 | 验证状态 | 阻塞状态 | 阻塞原因 / 解除条件 | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| F-001-I01 |  | `UI / Interaction / Data / Permission / State / Validation / Navigation / Integration / A11y / Performance / Other` | `范围内` | `是` |  |  |  |  |  | `未开始` | `未验证` | `未阻塞` |  |  |

Feature Item 应对应可以判断“是否实现、是否验证”的产品或工程行为，不要拆成 div、函数或变量级别。

#### Workstream / Slice 映射

| Workstream | Slice | Owner | 覆盖 Feature Items | 允许修改范围 | 依赖 | 队列状态 | 说明 |
|---|---|---|---|---|---|---|---|
| WS-001 | S-001 |  | F-001-I01, F-001-I02 |  |  | `待执行 / 当前执行 / 已阻塞 / 已完成` |  |

#### Feature 完成 Gate

- [ ] 所有必需 Feature Items 已识别
- [ ] 所有“范围内 + 必需=是”Items 均“已实施”
- [ ] 所有需要验证的必需 Items 均“通过 / 不适用”
- [ ] 不存在“已阻塞”的必需 Item
- [ ] 不存在两个活跃 Implementer 同时拥有同一个 Item
- [ ] Business Rule / Invariant 已验证
- [ ] Permission / Data Contract / Error Path 已覆盖
- [ ] APPROVED_CHANGE 已记录并验证目标行为
- [ ] Feature 生命周期已按 Item 状态重新计算

## 当前迁移焦点

- 活跃 Workstreams：
- 当前 Feature / Items：
- 待执行：
- 已阻塞：
- 下一候选：

## 开发时同步更新纪律

```text
选择 Workstream / Slice / Feature Items
→ 确认 Ownership 与并行安全
→ 确认范围状态 / 是否必需
→ 实施状态 = 实施中
→ 开发
→ 实施状态 = 已实施
→ 验证状态 = 验证中
→ 验证状态 = 通过 / 失败 / 不适用
→ 更新阻塞状态
→ 重新计算 Feature 生命周期
→ 更新 Module 汇总
→ 更新 Workstream / Slice Queue
→ 长周期任务同步 00-迁移状态总览
```

如果开发中发现新的必要子功能：立即加入清单、初始化状态、分配 Workstream / Owner，并重新判断并行安全和 Definition of Done。

如果实际代码状态与本清单冲突，以已确认 Decision、真实代码/Git/Runtime/Test 为依据，先重新校准清单再继续开发。
