# 00 迁移状态总览

> `.code-migration/` 的跨会话入口和摘要缓存。真实代码、测试、用户最新指令、`02` 生效规则、模块内 `01-模块功能点.md` 的 Atomic Item 事实、行为/体验验证和已确认 Decision 高于本摘要。

## 状态新鲜度

- **最后更新时间**：
- **最后更新者**：
- **状态新鲜度**：`最新 / 可能过期 / 需要重新校准`
- **Git 分支 / 最近提交**：
- **工作区状态**：`干净 / 有未提交改动 / 未知`

## 总体目标

- **总体目标**：
- **Target Behavior 摘要**：
- **用户新 UX / 交互 / 布局方案**：
- **总体风险**：`低风险 / 中风险 / 高风险`
- **总体状态**：`发现中 / 设计中 / 迁移中 / 切流中 / 清理中 / 已完成`
- **当前执行模式**：`单 Workstream / 受控并行 Workstream`

## 当前焦点

- **当前 Module**：
- **模块入口**：`modules/<module>/00-模块总览.md`
- **当前 Feature**：
- **当前 Atomic Item(s)**：
- **Feature 生命周期**：`已发现 / 已设计 / 实施中 / 验证中 / 已迁移 / 已清理`
- **Discovery / Provenance**：
- **行为验证**：
- **页面 / 交互验证**：
- **阻塞**：
- **下一安全动作**：

## Module 摘要

详细导航见 `03-迁移模块索引与进度.md`。模块化后的 Feature / Atomic Item 详细事实在 `modules/<module>/01-模块功能点.md`。

- [ ] [M-ORDER 订单中心](modules/order/00-模块总览.md) — `进行中 · 5/8 Feature`
- [x] [M-USER 用户管理](modules/user/00-模块总览.md) — `已完成`

| Module | 风险 | Feature 总数 | 已迁移 | 进行中 | 验证中 | 失败/阻塞 | 模块状态 | 模块入口 |
|---|---|---:|---:|---:|---:|---:|---|---|
|  |  | 0 | 0 | 0 | 0 | 0 | `未开始 / 进行中 / 验证中 / 已完成 / 已阻塞` |  |

## 活跃 Workstream

| Workstream | Module / Feature | Atomic Item(s) | 负责人 | 验证负责人 | 输入基线 | 临时 Batch | 状态 | 下一动作 |
|---|---|---|---|---|---|---|---|---|
| WS-001 |  |  |  |  |  |  | `待执行 / 当前执行 / 已阻塞 / 验证中 / 已完成` |  |

## 当前生效规则摘要

详细规则见 `02-迁移约束与开发规则.md`。

| 规则 ID | 作用域 | 强度 | 当前要求 | 状态 |
|---|---|---|---|---|
|  |  | `必须 / 禁止 / 必须询问 / 建议` |  | `已满足 / 未满足 / 待确认 / 不触发` |

## 行为 / 页面体验摘要

模块私有验证优先读取模块内 `03-源行为对齐.md` / `04-页面结构与交互.md`；全局或跨模块内容读取顶层 `05 / 06`。

| Module / Feature | WHAT | HOW / WHERE | 验证失败 | 最大未解决差异 |
|---|---|---|---:|---|
|  | `不要求 / 未验证 / 验证中 / 通过 / 失败` | `不要求 / 未验证 / 验证中 / 通过 / 失败` | 0 |  |

## 待执行 / 已阻塞 / 已延后

- **待执行**：
- **已延后**：

| Workstream / Feature / Atomic Item | 当前状态 | 阻塞原因 | 解除条件 |
|---|---|---|---|
|  | `已阻塞 / 验证失败` |  |  |

## 待澄清问题

| ID | 问题 | 类型 | 影响范围 | 状态 | 结论 |
|---|---|---|---|---|---|
| Q-001 |  | `必须询问 / 可推断 / 可延后` |  | `待处理 / 已回答 / 已延后` |  |

## 关键 Decision / Invariant

- **业务 Invariant**：
- **交互 Invariant**：
- **布局 Invariant**：
- **已批准业务变化**：
- **已批准交互 / 页面结构变化**：
- **目标架构**：

## 标准工作区索引

```text
00-迁移状态总览.md
01-迁移任务上下文.md
02-迁移约束与开发规则.md
03-迁移模块索引与进度.md
04-功能语义.md                  # 全局/跨模块按需
05-源行为与目标对齐清单.md      # 全局/跨模块按需
06-页面结构与交互对齐清单.md    # 全局/跨模块按需
07-功能融合映射.md              # 多来源按需
08-迁移决策记录.md              # 重大决策按需
09-迁移执行计划.md
modules/<module>/               # 复杂模块按需
```

模块目录：

```text
00-模块总览.md
01-模块功能点.md
02-功能语义.md           # 按需
03-源行为对齐.md         # 按需
04-页面结构与交互.md     # 按需
```

## 事实源优先级

1. 用户最新明确意图 / 新方案；
2. `02` 生效规则和 `08` 已确认 Decision / Target 产品契约；
3. 真实代码、Git、运行和测试；
4. 模块内 `01-模块功能点.md` 的 Atomic Item / 实施事实，或未模块化小模块在顶层 `03` 的事实；
5. 模块内 `03 / 04` 或顶层 `05 / 06` 的验证事实；
6. `09` Workstream / 输入基线；
7. 顶层 `03` 和本摘要的派生进度。

## 新对话恢复

1. 读取 `00`；
2. 读取 `01` 恢复目标与用户新方案；
3. 读取 `02` 生效规则；
4. 读取 `03` 找到当前 Module 入口；
5. 若模块化，进入 `modules/<module>/00`，再读取 `01-模块功能点.md`；
6. 按需读取模块内 `02–04` 或顶层 `04–08`；
7. 读取 `09` Workstream / 输入基线；
8. 核对 Git / 代码 / 测试；
9. 不一致时只重新校准受影响范围，并同步 Checkbox 投影。

## 上次会话交接

- **摘要**：
- **最后完成动作**：
- **当前模块入口**：
- **用户新方案变化**：
- **Discovery / Provenance**：
- **当前 Atomic Item 状态**：
- **行为 / 体验验证状态**：
- **测试 / 失败**：
- **未完成 / 未提交工作**：
- **活跃 Workstream / 输入基线**：
- **下一安全动作**：
